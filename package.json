{
  "name": "pelada-oficial",
  "version": "1.0.0",
  "private": true,
  "description": "Rei da Pelada — app Android (Capacitor) + site",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
